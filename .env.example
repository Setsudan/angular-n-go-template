# =============================================================================
# Angular + Go + Redis + PostgreSQL Template - Environment Variables
# =============================================================================
# Copy this file to .env and update the values for your local development
# cp env.example .env

# =============================================================================
# Database Configuration
# =============================================================================
# PostgreSQL connection string
# Format: postgres://username:password@host:port/database?sslmode=disable
DATABASE_URL=postgres://user:password@localhost:5432/angular_n_go_template?sslmode=disable

# Alternative database configuration (if not using DATABASE_URL)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=angular_n_go_template
# DB_USER=user
# DB_PASSWORD=password
# DB_SSLMODE=disable

# =============================================================================
# Redis Configuration
# =============================================================================
# Redis connection string
# Format: redis://username:password@host:port/database
REDIS_URL=redis://localhost:6379

# Alternative Redis configuration (if not using REDIS_URL)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=
# REDIS_DB=0

# =============================================================================
# JWT Authentication
# =============================================================================
# JWT secret key - CHANGE THIS IN PRODUCTION!
# Generate a secure random string: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# JWT token expiration time
# Examples: 1h, 24h, 7d, 30d
JWT_EXPIRY=24h

# =============================================================================
# Server Configuration
# =============================================================================
# Backend server port
PORT=8080

# Gin mode: debug, release, test
GIN_MODE=debug

# CORS origin for frontend
CORS_ORIGIN=http://localhost:4200

# =============================================================================
# Frontend Configuration
# =============================================================================
# API base URL for frontend
API_BASE_URL=http://localhost:8080/api/v1

# =============================================================================
# Development Settings
# =============================================================================
# Enable debug logging
DEBUG=true

# Enable request logging
ENABLE_REQUEST_LOGGING=true

# =============================================================================
# Docker Development (when using docker-compose)
# =============================================================================
# These are used when running with docker-compose
# The backend service will use these values when running in containers

# Database URL for Docker (connects to postgres service)
# DATABASE_URL=postgres://user:password@postgres:5432/angular_n_go_template?sslmode=disable

# Redis URL for Docker (connects to redis service)
# REDIS_URL=redis://redis:6379

# =============================================================================
# Production Settings (for reference)
# =============================================================================
# Uncomment and configure these for production deployment

# Database (Production)
# DATABASE_URL=postgres://prod_user:secure_password@prod_host:5432/prod_database?sslmode=require

# Redis (Production)
# REDIS_URL=redis://prod_user:secure_password@prod_host:6379/0

# JWT (Production)
# JWT_SECRET=your-very-secure-production-jwt-secret-key-here
# JWT_EXPIRY=1h

# Server (Production)
# PORT=8080
# GIN_MODE=release
# CORS_ORIGIN=https://yourdomain.com

# =============================================================================
# Optional: External Services
# =============================================================================
# Email service configuration (if implementing email features)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@yourdomain.com

# File upload configuration
# UPLOAD_DIR=./uploads
# MAX_UPLOAD_SIZE=10485760  # 10MB in bytes

# Logging configuration
# LOG_LEVEL=info
# LOG_FILE=./logs/app.log
